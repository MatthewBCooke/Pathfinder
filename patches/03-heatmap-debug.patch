--- a/gui/heatmap_widget.py
+++ b/gui/heatmap_widget.py
@@ -145,10 +145,23 @@ class HeatmapWidget(QWidget):
     def _plot_heatmap(self):
         """Generate occupancy heatmap with NaN/Inf checks"""
         self.ax.clear()
-
+        
         # Get filtered trials
         trials = self._get_filtered_trials()
+        
+        # === BEGIN DEBUG OUTPUT ===
+        import sys
+        print(f"\n🔍 HEATMAP DEBUG:", file=sys.stderr)
+        print(f"   Total trials in experiment: {len(self._experiment.trials) if self._experiment else 0}", file=sys.stderr)
+        print(f"   Filtered trials: {len(trials)}", file=sys.stderr)
         if not trials:
+            print(f"   ❌ No trials to plot!", file=sys.stderr)
+            self._plot_empty()
+            return
+        print(f"   First trial trajectory points: {len(trials[0].trajectory)}", file=sys.stderr)
+        print(f"   Pool center: {trials[0].pool_center}", file=sys.stderr)
+        print(f"   Pool diameter: {trials[0].pool_diameter}", file=sys.stderr)
+        # === END DEBUG OUTPUT ===
             self._plot_empty()
             return
